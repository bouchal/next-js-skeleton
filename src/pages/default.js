/**
 * Default page defined for loading in pageRouter, where you can decide for loading other one.
 */

import DefaultLayout from "../layouts/DefaultLayout";

export default (props) =>
{
	return <DefaultLayout />;
}